# Project Rules

- Do not run local compilation or build commands in this repository, including Gradle tasks such as `./gradlew`, `./gradlew.bat`, `build`, `check`, or `compileJava`.
- Use GitHub Actions or other CI results for build verification instead. Only run a local compilation or build when the user explicitly requests it.
